# __ECOSYSTEM_NAME__

This scaffold is incomplete by design. Before unattended execution:

1. Replace every `replace-` and `declare-` placeholder.
2. Write and test the evaluator before configuring an agent.
3. Add historical and adversarial reward-hacking fixtures.
4. Use separate role identities and stable role worktrees.
5. Configure external health and a deadman outside this host.
6. Exercise canary and rollback in a non-production environment.

Validate with the public kit's `scripts/validate.py`.
